# 问题 5.1：图 1 和图 2 的图注需要更加详细

## 问题

图 1 和图 2 的图注与说明应给出特征图维度或注意力输出尺寸，以提高可复现性。

## 回复

我们同意，并已同时修改图 1、图 2 的正文说明与图注，而不再只保留笼统的示意性描述。修订稿现在在图 1 中明确标出当前 PTB-XL 设置下的输入 `(B,12,1000)`、共享 backbone 输出 `(B,512,32)` 以及疾病分支输出；在图 2 中明确标出 learnable queries `(H,d)`、feature-derived keys/values `(B,L',d)`、attention scores `(B,H,L')` 与最终用于展示的 query-averaged explanation `(B,L')`。因此，这两幅图现在与实现中的张量接口和计算流程是逐项对应的。
